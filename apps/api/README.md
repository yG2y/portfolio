# API Placeholder

Este diretório contém um backend placeholder para a evolução do projeto.

Objetivos nesta fase:
- manter contrato de healthcheck (`GET /health`);
- servir como base para extrair serviços em `Node`, `Go` e `Kotlin`;
- integrar com Supabase futuramente sem quebrar o frontend.
